# Vision Tracker 실행방법

## 1. 제출 프로젝트 구성

이 프로젝트는 다음 세 장치로 구성됩니다.

- Jetson: YOLO TensorRT 검출, BoT-SORT 내부 ReID, OSNet 장기 ReID, 웹 서버 및 게임패드 제어
- 로직용 STM32F411RE: 팬·틸트 서보와 차량 모터 제어, UART 패킷 처리
- 외부 전원용 STM32: 전원 공급 역할만 하며 별도 펌웨어를 실행하지 않음

필수 모델 파일은 다음과 같습니다.

```text
AI/models/yolo11n-person.engine
AI/models/osnet_x0_25.engine
```

현재 실행 코드는 TensorRT `.engine`만 사용합니다. 엔진은 생성한 JetPack,
TensorRT 버전 및 GPU 환경에 종속됩니다. 제출 기준에서 원본 `.onnx`까지 요구하면
YOLO와 OSNet 변환에 사용한 원본 ONNX를 `AI/models/`에 추가해야 합니다. 현재
프로젝트에는 원본 ONNX 파일이 없습니다.

## 2. Jetson 가상환경 설치

TensorRT와 PyCUDA는 `pip`가 아니라 JetPack에 포함된 시스템 패키지를 사용합니다.
JetPack과 카메라 드라이버를 먼저 설치한 뒤 시스템 패키지를 볼 수 있는 가상환경을
생성합니다.

```bash
cd /home/aidl/work/Vision_Tracker_Project
python3 -m venv --system-site-packages AI/.venv
AI/.venv/bin/python -m pip install --upgrade pip
AI/.venv/bin/python -m pip install -r requirements.txt
```

설치 확인:

```bash
AI/.venv/bin/python -c "import cv2, serial, evdev, fastapi, uvicorn, tensorrt, pycuda.driver; print('OK')"
```

## 3. 게임패드와 장치 경로 설정

최초 한 번 게임패드 설정 파일을 만듭니다.

```bash
cp AI/gamepad.example.json AI/gamepad.local.json
```

`AI/gamepad.local.json`의 `enable`, `stop`, `slower`, `faster` 버튼 코드를 실제
게임패드 측정값으로 입력합니다. 제출 장비에서 사용 중인 `gamepad.local.json`은
제출 폴더에 포함합니다.

재부팅 후에도 유지되는 장치 경로를 확인합니다.

```bash
ls -l /dev/input/by-id/
ls -l /dev/serial/by-id/
```

## 4. 로직용 STM32 빌드와 업로드

외부 전원용 STM32에는 별도 프로그램을 업로드하지 않습니다. 다음 펌웨어는 로직용
NUCLEO-F411RE에만 업로드합니다.

```bash
cd /home/aidl/work/Vision_Tracker_Project/STM32
make clean
make
make run
```

`make run`은 연결된 STM32 플래시에 실제로 기록합니다. 완성된 제출 폴더에는 바로
업로드할 수 있도록 아래 두 파일을 포함합니다.

```text
STM32/rom_0x08000000.bin
STM32/rom_0x08000000.elf
```

## 5. Jetson 수동 실행

아래 장치 경로는 실제 장비의 `/dev/input/by-id/`와 `/dev/serial/by-id/` 경로로
바꿉니다.

```bash
cd /home/aidl/work/Vision_Tracker_Project/AI
.venv/bin/python main.py \
  --gamepad \
  --device /dev/input/by-id/<gamepad>-event-joystick \
  --config gamepad.local.json \
  --uart /dev/serial/by-id/<logic-stm32-uart>
```

브라우저에서 다음 주소로 접속합니다.

```text
http://<Jetson-IP>:8000
```

Jetson IP 확인:

```bash
ip -br -4 address
```

## 6. 부팅 자동 실행

프로젝트 루트에서 실제 장치 경로를 지정해 최초 한 번 설치합니다.

```bash
cd /home/aidl/work/Vision_Tracker_Project
VISION_PYTHON=/home/aidl/work/Vision_Tracker_Project/AI/.venv/bin/python \
VISION_GAMEPAD_DEVICE=/dev/input/by-id/<gamepad>-event-joystick \
VISION_UART_DEVICE=/dev/serial/by-id/<logic-stm32-uart> \
bash AUTOSTART/install.sh
```

운영 명령:

```bash
sudo systemctl restart vision-tracker-web.service
sudo systemctl status vision-tracker-web.service
sudo journalctl -u vision-tracker-web.service -n 100 --no-pager
```

## 7. 제출 폴더에 포함할 파일

```text
requirements.txt
실행방법.md
AI/main.py
AI/src/
AI/models/yolo11n-person.engine
AI/models/osnet_x0_25.engine
AI/gamepad.local.json
WEB/rc_web/
WEB/run.sh
AUTOSTART/install.sh
AUTOSTART/systemd/
STM32/Makefile
STM32의 펌웨어 소스·헤더·링커 스크립트·crt0.s
STM32/rom_0x08000000.bin
STM32/rom_0x08000000.elf
STM32_POWER/전원보드_설명.txt
```

원본 ONNX 파일을 확보하면 `AI/models/`에 함께 포함합니다.

## 8. 제출 폴더에서 제외할 파일

실행에 필요하지 않으므로 다음 항목은 제외합니다.

```text
.git/
.gitignore
AI/.venv/, AI/venv/, venv/
__pycache__/, *.pyc, .pytest_cache/
AI/tests/, WEB/tests/, WEB/tools/
NOTION_TROUBLESHOOTING/
AI/captures/ 및 촬영·다시보기 임시 데이터
WEB/requirements-dev.txt
STM32/*.o, STM32/*.d, STM32/*.map
STM32/__dump.txt, STM32/__dump_all.txt
STM32/servo_test_main.c 및 servo_test_* 산출물
```

문서는 `실행방법.md`만 남기고 `README.md`, `AI/*.md`, `WEB/*.md`,
`AUTOSTART/README.md` 등 개발·설명용 Markdown 파일은 제출본에서 제외해도 됩니다.
단, 소스 저장소 원본에서는 삭제하지 말고 제출용 복사본에서만 제외합니다.
